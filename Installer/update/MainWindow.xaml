<Window x:Class="update.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:update"
        mc:Ignorable="d"
        ThemeMode="System"
        Title="{DynamicResource AppTitle}" Height="400" Width="500" MinHeight="100" MinWidth="200"
        Icon="/update.png">
    <Window.Resources>
        <Style TargetType="Image">
            <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality" />
        </Style>
    </Window.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- Header: install path, version, language -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Width="{Binding ActualWidth, ElementName=ReleasesListBox}" x:Name="ChoosePathButton" Content="{DynamicResource ChooseInstallPath}" Click="ChoosePathButton_Click" ToolTip="{DynamicResource ChooseInstallPath_Tooltip}" />
                <TextBlock Grid.Column="1" x:Name="InstallPathText"  Margin="8,0,0,0" VerticalAlignment="Center"
                           TextWrapping="Wrap" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                           TextTrimming="CharacterEllipsis"
                           LineStackingStrategy="BlockLineHeight" LineHeight="16" MaxHeight="36" />
            </Grid>
            <StackPanel Orientation="Horizontal" Grid.Column="1">
                <ComboBox x:Name="LanguageComboBox" Width="Auto" Margin="8,0,0,0" SelectionChanged="LanguageComboBox_SelectionChanged" ToolTip="{DynamicResource Language_Tooltip}">
                    <ComboBoxItem x:Name="LanguageComboBoxEn" Tag="en">Eng</ComboBoxItem>
                    <ComboBoxItem x:Name="LanguageComboBoxRu" Tag="ru">Рус</ComboBoxItem>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Search and actions row -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Width="{Binding ActualWidth, ElementName=ReleasesListBox}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="SearchBox" HorizontalAlignment="Stretch" Height="22" VerticalAlignment="Center" Margin="0,0,8,0" ToolTip="{DynamicResource SearchTooltip}" TextChanged="SearchBox_TextChanged" />
                <Button Grid.Column="1" Padding="2" Height="{Binding ActualHeight, ElementName=SearchBox}" x:Name="RefreshButton" Click="RefreshButton_Click" ToolTip="{DynamicResource Refresh_Tooltip}">
                    <Image Source="/refresh.png"/>
                </Button>
            </Grid>
            <CheckBox Grid.Column="1" Margin="8,0,0,0" x:Name="IncludePrereleaseCheck" VerticalAlignment="Center" Unchecked="IncludePrereleaseCheck_Unchecked" Checked="IncludePrereleaseCheck_Checked" ToolTip="{DynamicResource IncludePrerelease_Tooltip}">
                <TextBlock TextTrimming="CharacterEllipsis" Text="{DynamicResource IncludePrerelease}" />
            </CheckBox>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="180"/>
                <ColumnDefinition Width="*" MinWidth="180"/>
            </Grid.ColumnDefinitions>
            <ListBox x:Name="ReleasesListBox" Grid.Column="0" SelectionMode="Single" ToolTip="{DynamicResource ReleasesList_Tooltip}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="0" Margin="1" Background="Transparent">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding TagName}" Foreground="Gray" Margin="0,2,8,0" />
                                    <TextBlock Text="{DynamicResource PreMarker}" Foreground="OrangeRed" Visibility="{Binding Prerelease, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <ScrollViewer Grid.Column="1" Margin="8,0,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid>
                    <Border Grid.Row="0" >
                        <Grid Visibility="{Binding SelectedItem, ElementName=ReleasesListBox, Converter={StaticResource NullToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="1" x:Name="ReleaseDetails" Text="{DynamicResource ReleaseDetails}" FontWeight="Bold" FontSize="16" ToolTip="{DynamicResource ReleaseDetails_Tooltip}"/>
                                <Button Grid.Column="0" Padding="2" Height="{Binding ActualHeight, ElementName=ReleaseDetails}" x:Name="ClearSelection" Click="ClearSelection_Click" Margin="0,0,4,0" ToolTip="{DynamicResource ClearSelection_Tooltip}">
                                    <Image Source="/close.png"/>
                                </Button>
                            </Grid>
                            <Grid Grid.Row="1" Margin="0,4,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" x:Name="ReleaseTitleText" Text="" FontWeight="Bold" FontSize="14" ToolTip="{DynamicResource ReleaseTitle_Tooltip}" />
                                <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,4,0,8">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{DynamicResource VersionLabel}" FontWeight="SemiBold" />
                                        <TextBlock Text=" " Width="8" />
                                        <TextBlock x:Name="ReleaseTagText" Text="" ToolTip="{DynamicResource ReleaseTag_Tooltip}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="{DynamicResource PrereleaseLabel}" FontWeight="SemiBold" />
                                        <TextBlock Text=" " Width="8" />
                                        <TextBlock x:Name="ReleasePrereleaseText" Text="" ToolTip="{DynamicResource ReleasePrerelease_Tooltip}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="{DynamicResource PublishedLabel}" FontWeight="SemiBold" />
                                        <TextBlock Text=" " Width="8" />
                                        <TextBlock x:Name="ReleasePublishedText" Text="" ToolTip="{DynamicResource ReleasePublished_Tooltip}" />
                                    </StackPanel>
                                </StackPanel>
                                <TextBox Grid.Row="2" x:Name="ReleaseBodyTextBox" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" ToolTip="{DynamicResource ReleaseBody_Tooltip}" />
                            </Grid>
                            <Button Grid.Row="2" Margin="0,8,0,0" x:Name="InstallButton" Content="{DynamicResource InstallSelected}" Click="InstallButton_Click" ToolTip="{DynamicResource InstallButton_Tooltip}" />
                        </Grid>
                    </Border>
                    <Border Visibility="{Binding SelectedItem, ElementName=ReleasesListBox, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}" Padding="8">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource CurrentInstallation}" FontWeight="Bold" FontSize="14" Margin="0,0,0,6" />
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="{DynamicResource InstalledVersionLabel}" FontWeight="SemiBold" />
                                <TextBlock x:Name="InstalledVersionText" Margin="8,0,0,0" ToolTip="{DynamicResource InstalledVersion_Tooltip}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="{DynamicResource AvailableReleaseLabel}" FontWeight="SemiBold" />
                                <TextBlock x:Name="AvailableVersionText" Margin="8,0,0,0" ToolTip="{DynamicResource AvailableVersion_Tooltip}" />
                            </StackPanel>
                            <StackPanel x:Name="LatestPrereleasePanel" Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="{DynamicResource LatestPrereleaseLabel}" FontWeight="SemiBold" />
                                <TextBlock x:Name="LatestPrereleaseText" Margin="8,0,0,0" ToolTip="{DynamicResource LatestPrerelease_Tooltip}" />
                            </StackPanel>
                            <TextBlock x:Name="NoSelectionInfoText" Text="{DynamicResource NoSelectionInfo}" TextWrapping="Wrap" Foreground="Gray" Margin="0,0,0,8" ToolTip="{DynamicResource NoSelectionInfo_Tooltip}" />
                            <Grid HorizontalAlignment="Left">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Button Grid.Row="0" HorizontalAlignment="Stretch" x:Name="RunButton" Content="{DynamicResource Run}" Click="Run_Click" Visibility="Collapsed" Margin="0,0,0,4" ToolTip="{DynamicResource RunButton_Tooltip}" />
                                <Button Grid.Row="1" HorizontalAlignment="Stretch" x:Name="DeleteButton" Content="{DynamicResource Delete}" Click="Delete_Click" Visibility="Collapsed" Margin="0,0,0,4" ToolTip="{DynamicResource DeleteButton_Tooltip}" />
                                <Button Grid.Row="2" HorizontalAlignment="Stretch" x:Name="NoSelInstallButton" Content="{DynamicResource InstallSelected}" Click="NoSelectionInstall_Click" Visibility="Collapsed" Margin="0,0,0,4" ToolTip="{DynamicResource NoSelInstallButton_Tooltip}" />
                                <Button Grid.Row="3" HorizontalAlignment="Stretch" x:Name="UpdateButton" Content="{DynamicResource UpdateToLatest}" Click="Update_Click" Visibility="Collapsed" Margin="0,0,0,4" ToolTip="{DynamicResource UpdateButton_Tooltip}"/>
                                <Button Grid.Row="4" HorizontalAlignment="Stretch" x:Name="RollbackToReleaseButton" Content="{DynamicResource RollbackToRelease}" Visibility="Collapsed" Click="RollbackToReleaseButton_Click" Margin="0,0,0,4" ToolTip="{DynamicResource RollbackToRelease_Tooltip}"/>
                                <Button Grid.Row="5" HorizontalAlignment="Stretch" x:Name="SwitchToPrereleaseButton" Content="{DynamicResource SwitchToPrerelease}" Visibility="Collapsed" Click="SwitchToPrereleaseButton_Click" ToolTip="{DynamicResource SwitchToPrerelease_Tooltip}" />
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </ScrollViewer>
        </Grid>

        <StatusBar Grid.Row="3" Margin="0,8,0,0" HorizontalContentAlignment="Stretch">
            <StatusBarItem HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <ProgressBar Margin="0,4,0,4" Grid.Row="0" x:Name="DownloadProgress" HorizontalAlignment="Stretch" Height="20" Minimum="0" Maximum="100" Value="0" Visibility="Collapsed" ToolTip="{DynamicResource DownloadProgress_Tooltip}" />
                    <Grid Grid.Row="1" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" x:Name="ProgressLabel" Text="" Margin="0,0,0,0" ToolTip="{DynamicResource ProgressLabel_Tooltip}" />
                        <TextBlock TextWrapping="Wrap" Grid.Column="1" x:Name="StatusText" Text="" Margin="8,0,8,0" ToolTip="{DynamicResource StatusText_Tooltip}" />
                    </Grid>
                </Grid>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
